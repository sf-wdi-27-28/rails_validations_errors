<h1>All Pets for <%= @owner.first_name %></h1>
<hr>

<% @pets.each do |pet| %>
  <%= pet.name %>
  <%= link_to "Show pet", owner_pet_path("#{@owner.id}","#{pet.id}"), class: "btn btn-info" %>
<% end %>

<!-- "/owners/#{@owner.id}/pets/#{pet.id}" -->
